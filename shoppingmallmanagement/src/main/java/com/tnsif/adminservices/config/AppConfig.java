package com.tnsif.adminservices.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // Future configuration beans (like CORS, Security, etc.)
}
